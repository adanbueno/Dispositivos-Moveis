<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ufcquixada.componentesbasicos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Toggle_button"></activity>
        <activity android:name=".Edittext"></activity>
        <activity android:name=".Autocomplete"></activity>
        <activity android:name=".Spinner_list"></activity>
        <activity android:name=".Radio_buttons"></activity>
        <activity android:name=".Long_press"></activity>


    </application>

</manifest>